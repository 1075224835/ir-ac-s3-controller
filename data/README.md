# LittleFS data directory

This directory is reserved for files uploaded to the ESP32 LittleFS partition.

The current firmware stores runtime WiFi settings, learned IR commands, and sleep curve data on the device after you configure them from the web UI. Those device-side files are not automatically exported into this source directory.

